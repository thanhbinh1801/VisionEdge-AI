fastapi>=0.100.0
uvicorn>=0.22.0
pytest>=7.0.0
numpy>=1.24.0
opencv-python>=4.8.0
# Đường đọc biển số chính: recognizer chuyên biển chạy trên ONNXRuntime (CPU).
# Đo trên clip cổng: 5/5 lượt xe đọc đúng, ~45ms mỗi tấm biển.
fast-plate-ocr>=1.0.0
onnxruntime>=1.20.0
# Dự phòng khi fast-plate-ocr không cài được. Cùng clip đó EasyOCR đọc được 2/5 lượt và
# trượt cả hai biển vuông 2 dòng, nên đừng đảo lại thứ tự ưu tiên.
easyocr>=1.7.0
python-multipart>=0.0.6
requests>=2.31.0
google-genai>=1.0.0
